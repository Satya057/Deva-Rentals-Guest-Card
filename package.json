{
  "name": "guest-card-deva-rentals",
  "version": "1.0.0",
  "private": true,
  "description": "Tenant pre-screening form with Gmail (app password) and optional Google Calendar API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "nodemailer": "^6.9.16"
  }
}
